# flatpak

A way to ship cross-platform Linux applications, similar to snap.

- search:
`flatpak search {{...}}`

- install:
`flatpak install {{...}}`

- run:
`flatpak run {{...}}`

- list installed:
`flatpak list`

- uninstall all:
`flatpak uninstall --all`
